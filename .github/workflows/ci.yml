name: CI

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
    workflow_dispatch:

env:
    FORCE_COLOR: true

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Hello
              run: echo "CI is working"
